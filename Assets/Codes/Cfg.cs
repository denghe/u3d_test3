using System.Collections.Generic;
using UnityEngine;

public static class Cfg {
    public static ItemConfig[] items;
    // todo: 各种分类容器, 每种怪针对每种 item 的 掉率??
    // 词条??? 随机属性????

    public static void Init() {
        Debug.Assert(Res.sprites_bg != null);
        Debug.Assert(items == null);
        //items = 


        // todo: fill


    }
}
